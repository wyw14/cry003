openapi: 3.0.3
info:
  title: 实时文件扫描与进度中心 API
  version: 1.0.0
paths:
  /api/v1/items:
    get:
      responses:
        '200': {description: scoped items}
    post:
      responses:
        '201': {description: created idempotently}
  /healthz:
    get:
      responses:
        '200': {description: healthy}
